@model DepositoViewModel
@{
    ViewData["Title"] = "Depósitos";
}

<div class="row justify-content-center">
    <div class="col-12 col-lg-7">
        <div class="card border-0 shadow-sm" style="border-left: 4px solid #95bf5a;">
            <div class="card-body p-4">
                <div class="d-flex align-items-start justify-content-between gap-3">
                    <div>
                        <h1 class="h5 mb-1 fw-semibold" style="color: #2661a9;">Registrar depósito</h1>
                        <div class="text-muted small">Se registrará un movimiento de entrada</div>
                    </div>
                </div>

                <form asp-action="Deposito" method="post" class="mt-3" data-activar-spinner="true" novalidate>
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>

                    <div class="row g-3">
                        <div class="col-12 col-md-6">
                            <label asp-for="Cuenta" class="form-label fw-semibold"></label>
                            <input asp-for="Cuenta" class="form-control" maxlength="8" style="border-color: #2661a9; border-width: 1px;" />
                            <span asp-validation-for="Cuenta" class="text-danger small"></span>
                        </div>
                        <div class="col-12 col-md-6">
                            <label asp-for="Importe" class="form-label fw-semibold"></label>
                            <input asp-for="Importe" class="form-control" inputmode="decimal" style="border-color: #2661a9; border-width: 1px;" />
                            <span asp-validation-for="Importe" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn" style="background-color: #95bf5a; color: white; border: none;" data-boton-spinner>
                            <span class="texto-normal">Registrar</span>
                            <span class="texto-cargando d-none">
                                <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                                Enviando...
                            </span>
                        </button>
                        <a class="btn btn-outline-primary" style="color: #2661a9; border-color: #2661a9; border-width: 2px;" asp-controller="Panel" asp-action="Index">Volver</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
